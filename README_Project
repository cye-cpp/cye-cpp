# Personal Biography Website
Built with react

## Commands
```
npm start
```
To start the development server

```
npm run deploy
```
To build static page and deploy on the `gh-pages` branch

```
git add .
git commit -m "<title>"
git push origin master
```
To push a commit to the `master` branch


## Known issues
- using devTools and loading page will trigger the "about me" initial animation